version: "3"

services:
  test-mock:
    build: .
    container_name: tset_mock
    ports:
      - "5000:5000"
    networks:
      - test_network
    volumes:
      - ./:/app
    command: python app.py

networks:
  test_network: